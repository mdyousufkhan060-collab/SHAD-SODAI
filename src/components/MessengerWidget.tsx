import React from 'react';

// Floating chat / messenger widget has been permanently removed in accordance with UI requirements.
// Support is directly and fully accessible via the Navigation Bar / Support page (#/support).
export const MessengerWidget: React.FC = () => {
  return null;
};

